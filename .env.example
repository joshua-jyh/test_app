RAILS_ENV=development
PORT=3000
RAILS_MAX_THREADS=5

# staging / production MySQL
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=test_app_staging
DB_USERNAME=test_app
DB_PASSWORD=

# Set true only after HTTPS is configured at the reverse proxy.
FORCE_SSL=false
RAILS_SERVE_STATIC_FILES=true
